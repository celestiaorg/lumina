{
  "devDependencies": {
    "@noble/curves": "^2.0.0",
    "@vitest/browser": "^3.2.4",
    "playwright": "^1.54.1",
    "typescript": "^5.9.2",
    "vite-plugin-wasm": "^3.5.0"
  },
  "scripts": {
    "tsc": "tsc",
    "pretest": "for crate in types grpc; do wasm-pack build $crate --all-features; done",
    "test": "vitest --no-watch"
  }
}
