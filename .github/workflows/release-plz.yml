name: Release-plz

permissions:
  pull-requests: write
  contents: write

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      mode:
        description: "Release mode"
        required: true
        default: "rc"
        type: choice
        options:
          - rc
          - final

jobs:
  release-plz:
    name: Release-plz
    runs-on: ubuntu-latest

    outputs:
      pr: ${{ steps.release-plz.outputs.pr }}
      prs_created: ${{ steps.release-plz.outputs.prs_created }}
      node_rc_prefix: ${{ steps.release-plz.outputs.node_rc_prefix }}
      releases: ${{ steps.release-plz.outputs.releases }}
      releases_created: ${{ steps.release-plz.outputs.releases_created }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_PLZ_TOKEN }}

      - name: Setup toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          target: wasm32-unknown-unknown
          # this disables default behavior of `setup-rust-toolchain` to put `-D warnings` which overwrites `.cargo/config.toml`
          rustflags: ""
          
      - name: Run release-plz
        id: release-plz
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_PLZ_TOKEN }}
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
          RUST_LOG: info
          INPUT_MODE: ${{ github.event_name == 'workflow_dispatch' && inputs.mode || 'rc' }}
        run: |
          set -euo pipefail
          cargo xtask gha release \
            --mode "$INPUT_MODE" \
            --gha-output

      - name: Install wasm-pack
        if: steps.release-plz.outputs.prs_created == 'true'
        uses: taiki-e/install-action@v2
        with:
          tool: wasm-pack@0.13.1

      - name: Bump lumina-node npm version
        if: steps.release-plz.outputs.prs_created == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_PLZ_TOKEN }}
          PR_JSON: ${{ steps.release-plz.outputs.pr }}
          NODE_RC_PREFIX: ${{ steps.release-plz.outputs.node_rc_prefix }}
          RUST_LOG: info
        run: |
          set -euo pipefail
          cargo xtask gha npm-update-pr \
            --pr-json "$PR_JSON" \
            --node-rc-prefix="$NODE_RC_PREFIX"

  release-npm:
    name: Release-npm
    runs-on: ubuntu-latest

    needs:
    - release-plz

    if: success() && needs.release-plz.outputs.releases_created == 'true'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_PLZ_TOKEN }}

      - name: Setup toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          target: wasm32-unknown-unknown
          rustflags: ""

      - name: Install wasm-pack
        uses: taiki-e/install-action@v2
        with:
          tool: wasm-pack@0.13.1

      - name: Publish to npm
        env:
          NPM_REGISTRY_TOKEN: ${{ secrets.NPM_REGISTRY_TOKEN }}
          RUST_LOG: info
        run: |
          set -euo pipefail
          cargo xtask gha npm-publish

  release-uniffi:
    name: Release-uniffi
    runs-on: macos-latest

    needs:
    - release-plz

    if: >-
      ${{
        success()
        && contains(
          fromJSON(needs.release-plz.outputs.releases).*.package_name,
          'lumina-node-uniffi'
        )
      }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_PLZ_TOKEN }}

      - name: Setup toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          target: "\
            aarch64-apple-ios,\
            aarch64-apple-ios-sim,\
            aarch64-linux-android,\
            armv7-linux-androideabi,\
            x86_64-linux-android,\
            i686-linux-android"

      - name: Install cargo-ndk
        uses: taiki-e/cache-cargo-install-action@v2
        with:
          tool: cargo-ndk@3.5

      - name: Build uniffi artifacts
        id: uniffi
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_PLZ_TOKEN }}
          RELEASES_JSON: ${{ needs.release-plz.outputs.releases }}
          RUST_LOG: info
        run: |
          set -euo pipefail
          cargo xtask gha uniffi-release \
            --releases-json "$RELEASES_JSON" \
            --gha-output

      - name: Upload uniffi artifacts to GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.uniffi.outputs.uniffi_tag }}
          overwrite_files: true
          files: |
            lumina-node-uniffi-ios.tar.gz
            lumina-node-uniffi-android.tar.gz
            lumina-node-uniffi-sha256-checksums.txt
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_PLZ_TOKEN }}
